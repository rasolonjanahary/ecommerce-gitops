apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ecommerce
resources:
  - statefulset.yaml
  - service.yaml
configMapGenerator:
  - name: postgres-init
    files:
      - init.sql
